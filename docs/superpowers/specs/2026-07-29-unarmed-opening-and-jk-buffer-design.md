# 空手开场与 J-K 连招缓冲设计

保安处任务完成并由王子毅授予帝王剑前，杨子豪的 Canvas 角色不绘制剑，战斗输入仍按现有锁定规则禁用。

`J` 突进开始后，玩家在位移阶段按下 `K` 会缓存一次升龙请求。突进位移结束的同一更新帧立即执行连招升龙，继承突进连招窗口、刷新 J 冷却并获得龙血；不再等待 J 动画的剩余锁定时间。非突进状态下的 K 维持现有独立升龙行为。

验证：检查未装备帝王剑时不进入剑绘制分支；检查缓存 K 后在 `dashMotion` 完结时调用连招升龙；运行完整测试和语法检查。
